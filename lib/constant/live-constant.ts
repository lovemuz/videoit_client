const LIVE_CONSTANT: {
    MODAL_STATE_DEFAULT: number;
    MODAL_STATE_PROFILE: number;
    MODAL_STATE_CALLING: number;
} = (function () {
    return {
        MODAL_STATE_DEFAULT: 0,
        MODAL_STATE_PROFILE: 1,
        MODAL_STATE_CALLING: 2,
    }
}())

export {
    LIVE_CONSTANT,
}